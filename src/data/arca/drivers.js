/**
 * ARCA Menards Series - Default Driver Roster
 * This is the starting point for the ARCA series driver list.
 * Drivers will be added as they join the series.
 */

export const defaultArcaDrivers = [
  // Placeholder - drivers will be added as series is populated
  // Structure should match: { id, number, name, team, manufacturer, driver_type, status, retired, points, wins, top3, top5, dnfs }
];
